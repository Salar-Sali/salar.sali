import WorkContainer from "../components/WorkPage/WorkContainer";
import MainTemplate from "./MainTemplate";

function Work() {
  return (
    <MainTemplate>
      <WorkContainer />
    </MainTemplate>
  );
}

export default Work;
